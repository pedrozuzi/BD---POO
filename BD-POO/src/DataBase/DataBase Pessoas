CREATE DATABASE PESSOAS

USE PESSOAS

CREATE TABLE PESSOA (
	ID BIGINT UNIQUE NOT NULL,
	NOME VARCHAR(100) UNIQUE)
	
INSERT INTO PESSOA VALUES
	(1, 'PEDRO'),
	(2, 'MARIA')
	
	SELECT * FROM PESSOA